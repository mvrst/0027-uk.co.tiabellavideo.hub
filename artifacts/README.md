# Artifacts

Release-facing metadata and non-app asset mirrors for **`0027-uk.co.tiabellavideo.hub`** (`uk.co.tiabellavideo.hub`).

| Path | Purpose |
|------|---------|
| `metadata.json` | Repository / app ids, pointers to design and listing assets |
| `release_notes_v*.md` | Human-readable release notes per version |
| `design/0027.pen` | Pencil design source (authoritative layout reference) |
| `store-assets/` | Google Play listing images (icon, feature graphic, etc.) |
| `web-handoff/web/` | Static web mirror / export helper (not the shipped Flutter app) |

The **Flutter application** lives only under [`../app/`](../app/).
